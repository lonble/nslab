http_port 3128 intercept
https_port 3129 intercept ssl-bump generate-host-certificates=off cert=/etc/squid/ca.pem

acl step1 at_step SslBump1
ssl_bump peek step1
ssl_bump splice all

http_access allow all

[Unit]
Description=Restic REST Server (Append-Only)
After=network.target

[Service]
ExecStart=/usr/local/bin/rest-server --path /srv/backup --append-only --listen :8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
